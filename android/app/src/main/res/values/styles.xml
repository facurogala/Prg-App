<resources xmlns:tools="http://schemas.android.com/tools">
  <!-- Modificación del tema principal para soportar DayNight -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Personaliza tu tema aquí -->
    <item name="android:textColor">@android:color/black</item>
    <item name="android:editTextStyle">@style/ResetEditText</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:statusBarColor">?attr/colorPrimaryDark</item> <!-- Cambia el color de la barra de estado -->
    <!-- Añadir otros estilos de acuerdo a tu diseño -->
  </style>

  <!-- Estilo para EditText personalizado -->
  <style name="ResetEditText" parent="@android:style/Widget.EditText">
    <item name="android:padding">0dp</item>
    <item name="android:textColorHint">#c8c8c8</item>
    <item name="android:textColor">@android:color/black</item>
  </style>

  <!-- Estilo para la pantalla de splash -->
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/splashscreen</item>
  </style>
</resources>
